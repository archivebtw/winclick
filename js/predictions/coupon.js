(()=>{'use strict';
function getOdd(m,k){return window.WLMarkets?WLMarkets.odd(m,k):Number(m?.odds?.[k]);}
function combinedOdd(){const s=WLState.data,ids=Object.keys(s.selections);return ids.reduce((total,id)=>{const m=s.matches.find(x=>x.id===id);return total*(m?getOdd(m,s.selections[id]):1);},ids.length?1:0);}
function select(matchId,market,type){const s=WLState.data;if(type==='single')s.selections={};s.selections[matchId]=market;s.feedMatchId=matchId;}
function remove(matchId){delete WLState.data.selections[matchId];}function clear(){WLState.data.selections={};}
function place(type,stake){const s=WLState.data,ids=Object.keys(s.selections),amount=Math.floor(Number(stake)||0),now=Date.now();if(!ids.length)return{ok:false,message:'Выберите исход.'};if(type==='single'&&ids.length!==1)return{ok:false,message:'Для одиночного нужен один матч.'};if(type==='express'&&ids.length<2)return{ok:false,message:'Для экспресса нужно минимум 2 матча.'};if(amount<10)return{ok:false,message:'Минимальная сумма — 10 жетонов.'};if(amount>s.tokens)return{ok:false,message:'Недостаточно жетонов.'};const picks=ids.map(id=>{const m=s.matches.find(x=>x.id===id),market=s.selections[id];return{matchId:id,market,odds:getOdd(m,market),title:m?`${m.home} — ${m.away}`:''};});for(const p of picks){const m=s.matches.find(x=>x.id===p.matchId);if(!m||!Number.isFinite(p.odds)||p.odds<=0)return{ok:false,message:'Один из коэффициентов недоступен.'};const gate=window.WLMarkets?WLMarkets.canBet(m,now):{ok:m.status!=='FINISHED',reason:'Линия закрыта'};if(!gate.ok)return{ok:false,message:`Ставка не принята: ${gate.reason}.`};}const totalOdds=picks.reduce((a,p)=>a*p.odds,1);s.tokens-=amount;s.tickets.push({id:now,stake:amount,totalOdds,picks,status:'ACTIVE',payout:0});s.selections={};s.quests.predictions++;return{ok:true,message:'Купон принят. Коэффициент зафиксирован в момент ставки.'};}
window.WLCoupon={combinedOdd,select,remove,clear,place};
})();
