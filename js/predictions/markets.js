(()=>{'use strict';
const clamp=(n,a,b)=>Math.max(a,Math.min(b,n));
const round=n=>Math.round(clamp(n,1.05,18)*100)/100;
const DEF={P1:{label:'П1',group:'Исход'},X:{label:'Ничья',group:'Исход'},P2:{label:'П2',group:'Исход'},'1X':{label:'1X',group:'Двойной шанс'},'12':{label:'12',group:'Двойной шанс'},'X2':{label:'X2',group:'Двойной шанс'},O15:{label:'ТБ 1.5',group:'Тоталы'},U15:{label:'ТМ 1.5',group:'Тоталы'},O25:{label:'ТБ 2.5',group:'Тоталы'},U25:{label:'ТМ 2.5',group:'Тоталы'},BTTS_Y:{label:'Обе забьют — Да',short:'ОЗ Да',group:'Голы'},BTTS_N:{label:'Обе забьют — Нет',short:'ОЗ Нет',group:'Голы'},H_O05:{label:'Хозяева ТБ 0.5',short:'Хоз. забьют',group:'Голы'},A_O05:{label:'Гости ТБ 0.5',short:'Гости забьют',group:'Голы'}};
const KEYS=Object.keys(DEF),MAIN=['P1','X','P2'];
function poissonCDF(k,lambda){let term=Math.exp(-lambda),sum=term;for(let i=1;i<=k;i++){term*=lambda/i;sum+=term;}return clamp(sum,0,1);}
function normalize(o,keys=Object.keys(o)){const s=keys.reduce((a,k)=>a+Math.max(.0001,Number(o[k])||0),0),r={};keys.forEach(k=>r[k]=Math.max(.0001,Number(o[k])||0)/s);return r;}
function factualMain(m){return normalize(m.fairProbabilities||{P1:.38,X:.27,P2:.35},MAIN);}
function probabilities(m,ts=Date.now()){
 const base=factualMain(m),live=m.status==='LIVE',elapsed=live?clamp((ts-m.start)/(m.end-m.start),0,1):0,goals=(Number(m.scoreH)||0)+(Number(m.scoreA)||0),remaining=1-elapsed;
 let p1=base.P1,x=base.X,p2=base.P2;if(live){const d=(m.scoreH||0)-(m.scoreA||0),w=.12+.72*elapsed;p1=p1*(1-w)+(d>0?.82:d<0?.06:.31)*w;p2=p2*(1-w)+(d<0?.82:d>0?.06:.31)*w;x=x*(1-w)+(d===0?.40:.12)*w;({P1:p1,X:x,P2:p2}=normalize({P1:p1,X:x,P2:p2},MAIN));}
 const strengthH=(Number(m.homeAttack)||80)/Math.max(1,Number(m.awayDefense)||80),strengthA=(Number(m.awayAttack)||80)/Math.max(1,Number(m.homeDefense)||80),lambdaH=Math.max(.04,(1.30*strengthH+.18)*remaining),lambdaA=Math.max(.04,(1.08*strengthA+.15)*remaining),lambda=lambdaH+lambdaA,need15=Math.max(0,2-goals),need25=Math.max(0,3-goals),over15=need15<=0?1:1-poissonCDF(need15-1,lambda),over25=need25<=0?1:1-poissonCDF(need25-1,lambda),hHas=(m.scoreH||0)>0?1:1-Math.exp(-lambdaH),aHas=(m.scoreA||0)>0?1:1-Math.exp(-lambdaA),btts=hHas*aHas;
 return{P1:p1,X:x,P2:p2,'1X':p1+x,'12':p1+p2,'X2':x+p2,O15:over15,U15:1-over15,O25:over25,U25:1-over25,BTTS_Y:btts,BTTS_N:1-btts,H_O05:hHas,A_O05:aHas};
}
function fairOdd(p,margin=.93){return round(margin/Math.max(.025,p));}
function suspensionReason(m,ts=Date.now()){if(m.status==='FINISHED')return'Матч завершён';if(Number(m.marketSuspendedUntil||0)>ts)return m.marketSuspendReason||'Пересчёт линии';if(m.status==='LIVE'&&m.end-ts<=5000)return'Финальный свисток';return'';}
function marketOpen(m,ts=Date.now()){return !suspensionReason(m,ts)&&(m.status==='LIVE'||(m.status==='UPCOMING'&&ts>=Number(m.marketOpenAt||0)));}
function suspend(m,reason='Пересчёт линии',duration=2400,ts=Date.now()){m.marketSuspendedUntil=Math.max(Number(m.marketSuspendedUntil||0),ts+duration);m.marketSuspendReason=reason;m.market=m.market||{};m.market.label=`⏸ ${reason}`;}
function canBet(m,ts=Date.now()){const reason=suspensionReason(m,ts);if(reason)return{ok:false,reason};if(!marketOpen(m,ts))return{ok:false,reason:'Линия ещё не открыта'};return{ok:true,reason:''};}
function stage(m,ts=Date.now()){if(m.status==='FINISHED')return'closed';if(Number(m.marketSuspendedUntil||0)>ts)return'suspended';if(m.status==='LIVE')return'live';if(ts<Number(m.marketOpenAt||0))return'collecting';if(!m.marketOpenedAt)return'opening';return'prematch';}
function ensure(m,ts=Date.now()){const p=probabilities(m,ts);if(!m.extraOdds)m.extraOdds={};if(!m.extraOddsTarget)m.extraOddsTarget={};if(!m.extraOddsTrend)m.extraOddsTrend={};if(!m.marketBets)m.marketBets={P1:0,X:0,P2:0};KEYS.forEach(k=>{if(MAIN.includes(k))return;if(!Number.isFinite(Number(m.extraOdds[k])))m.extraOdds[k]=marketOpen(m,ts)?fairOdd(p[k]):0;if(!Number.isFinite(Number(m.extraOddsTarget[k])))m.extraOddsTarget[k]=m.extraOdds[k];if(!Number.isFinite(Number(m.extraOddsTrend[k])))m.extraOddsTrend[k]=0;});if(!Number.isFinite(m.nextPublicBetPulse))m.nextPublicBetPulse=ts+900;return m;}
function openMarket(m,ts,p){m.marketOpenedAt=ts;MAIN.forEach(k=>{const o=fairOdd(p[k]);m.odds[k]=o;m.oddsTarget[k]=o;m.oddsTrend[k]=0;m.marketBets[k]=8+Math.floor(Math.random()*12);});KEYS.filter(k=>!MAIN.includes(k)).forEach(k=>{const o=fairOdd(p[k]);m.extraOdds[k]=o;m.extraOddsTarget[k]=o;m.extraOddsTrend[k]=0;});m.publicBetVolume=MAIN.reduce((a,k)=>a+m.marketBets[k],0);m.lastBetBatch=m.publicBetVolume;m.marketActivityUntil=ts+6000;m.market=m.market||{};m.market.label='🟢 Предматчевая линия открыта';}
function pulseMain(m,p,ts){const crowdBias={P1:1+(Math.random()-.5)*.18,X:1+(Math.random()-.5)*.14,P2:1+(Math.random()-.5)*.18},weights=normalize({P1:p.P1*crowdBias.P1,X:p.X*crowdBias.X,P2:p.P2*crowdBias.P2},MAIN),batch=25+Math.floor(Math.random()*170);let remaining=batch;MAIN.forEach((k,i)=>{const add=i===2?remaining:Math.max(1,Math.round(batch*weights[k]));remaining-=add;m.marketBets[k]=(m.marketBets[k]||0)+Math.max(0,add);});const total=MAIN.reduce((a,k)=>a+m.marketBets[k],0),shares=normalize(m.marketBets,MAIN);MAIN.forEach(k=>{const demandShift=(shares[k]-p[k])*.42,implied=clamp(p[k]+demandShift,.035,.90),next=round(.93/implied);m.oddsTrend[k]=next<m.odds[k]?-1:next>m.odds[k]?1:0;m.oddsTarget[k]=next;});m.lastBetBatch=batch;m.publicBetVolume=total;m.preMatchBetSplit=Object.fromEntries(MAIN.map(k=>[k,Math.round(shares[k]*100)]));m.marketActivityUntil=ts+4500;m.market=m.market||{};const top=MAIN.slice().sort((a,b)=>shares[b]-shares[a])[0],label=top==='P1'?m.home:top==='P2'?m.away:'ничью';m.market.label=`💸 Поток ставок: ${label}`;}
function pulseExtras(m,p){KEYS.filter(k=>!MAIN.includes(k)).forEach((k,i)=>{const fair=fairOdd(p[k]),demand=(Math.random()-.5)*.055+((i%3)-1)*.004,next=round(fair*(1+demand));m.extraOddsTrend[k]=next<m.extraOdds[k]?-1:next>m.extraOdds[k]?1:0;m.extraOddsTarget[k]=next;});}
function animate(m){MAIN.forEach(k=>{const cur=Number(m.odds[k]),tar=Number(m.oddsTarget[k]);if(!Number.isFinite(cur)||!Number.isFinite(tar)||cur<=0)return;const d=tar-cur;if(Math.abs(d)<.005){m.odds[k]=round(tar);m.oddsTrend[k]=0;}else{m.odds[k]=round(cur+d*.20);m.oddsTrend[k]=d<0?-1:1;}});KEYS.filter(k=>!MAIN.includes(k)).forEach(k=>{const cur=Number(m.extraOdds[k]),tar=Number(m.extraOddsTarget[k]);if(!Number.isFinite(cur)||!Number.isFinite(tar)||cur<=0)return;const d=tar-cur;if(Math.abs(d)<.005){m.extraOdds[k]=round(tar);m.extraOddsTrend[k]=0;}else{m.extraOdds[k]=round(cur+d*.20);m.extraOddsTrend[k]=d<0?-1:1;}});}
function update(m,ts=Date.now()){ensure(m,ts);const p=probabilities(m,ts);if(Number(m.marketSuspendedUntil||0)>ts){animate(m);return;}if(!marketOpen(m,ts)){MAIN.forEach(k=>{m.odds[k]=0;m.oddsTarget[k]=0;m.oddsTrend[k]=0;});KEYS.filter(k=>!MAIN.includes(k)).forEach(k=>{m.extraOdds[k]=0;m.extraOddsTarget[k]=0;m.extraOddsTrend[k]=0;});m.market=m.market||{};m.market.label='⏳ Приём первых заявок';return;}if(!m.marketOpenedAt)openMarket(m,ts,p);if(ts>=m.nextPublicBetPulse&&m.status!=='FINISHED'){m.nextPublicBetPulse=ts+(m.status==='UPCOMING'?1000+Math.random()*2100:1800+Math.random()*3500);pulseMain(m,p,ts);pulseExtras(m,p);}animate(m);}
function odd(m,key){return MAIN.includes(key)?Number(m.odds?.[key]):Number(m.extraOdds?.[key]);}
function trend(m,key){return MAIN.includes(key)?Number(m.oddsTrend?.[key]||0):Number(m.extraOddsTrend?.[key]||0);}
function label(key,short=false){return short?(DEF[key]?.short||DEF[key]?.label||key):(DEF[key]?.label||key);}
function won(m,key){const h=Number(m.scoreH)||0,a=Number(m.scoreA)||0,t=h+a;switch(key){case'P1':return h>a;case'X':return h===a;case'P2':return h<a;case'1X':return h>=a;case'12':return h!==a;case'X2':return h<=a;case'O15':return t>1.5;case'U15':return t<1.5;case'O25':return t>2.5;case'U25':return t<2.5;case'BTTS_Y':return h>0&&a>0;case'BTTS_N':return h===0||a===0;case'H_O05':return h>0;case'A_O05':return a>0;default:return false;}}
window.WLMarkets={DEF,KEYS,ensure,update,odd,trend,label,won,probabilities,marketOpen,stage,canBet,suspend,suspensionReason};
})();
